package com.yangmf.mini_nodepad.aiservice;

import dev.langchain4j.service.SystemMessage;
import dev.langchain4j.service.UserMessage;

public interface PageAssistant {

    /**
     * AI 生成摘要：受 @Size(max = 500) 约束
     * 核心策略：限制字数，强制剥离无关废话，聚焦核心骨架
     */
    @SystemMessage("你是一个企业级的知识管理专家，擅长从海量杂乱文本中进行极简信息压缩与核心抽取。")
    @UserMessage("""
    请对以下正文内容进行专业、严谨的摘要总结。
    
    【执行规则】：
    1. 核心提炼：精准提取正文的核心主旨、关键结论或核心步骤。过滤掉所有寒暄、无意义的口水话及多余的修饰词。
    2. 严格限长：摘要字数必须严格控制在 100 到 400 个字符之间（绝对不可超过500字极限）。
    3. 纯净输出：严禁输出任何废话前缀（如"好的"、"摘要如下："、"根据提供的内容"等），必须直接输出摘要的正文本身。
    4. 结构直白：采用平铺直叙的专业陈述语气，确保逻辑连贯。
    
    正文内容：
    {{it}}
    """)
    String generateSummary(String text);

    /**
     * AI 生成标题：受 @Size(max = 200) 约束
     * 核心策略：要求极致简短（通常书签标题30个字以内最好），禁止特殊符号
     */
    @SystemMessage("你是一个专业的编辑和数据清洗机器人，擅长为长文本生成高度精炼、画龙点睛的标题。")
    @UserMessage("""
    请根据以下正文内容，生成一个高度精炼的标题。
    
    【执行规则】：
    1. 极致简炼：标题字数建议在 10 到 30 个字符之间（系统硬性上限为200字符，严禁超标）。
    2. 一语中的：用最专业的词汇，准确概括文本的最核心特征。
    3. 纯净输出：
       - 绝对禁止包含说明性废话（如"标题："、"生成结果："等）。
       - 绝对禁止在标题外层添加任何引号（""、''）或书名号（《》）。
       - 仅需返回纯文本内容本身。
       
    正文内容：
    {{it}}
    """)
    String generateTitle(String text);
}